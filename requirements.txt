# ── Core AI / RL ──────────────────────────────────────────────────────────────
torch>=2.0.0          # Neural network (policy + value heads), MCTS inference
numpy>=1.24.0         # Board encoding, action masks, replay buffer arrays

# ── Web backend ───────────────────────────────────────────────────────────────
fastapi>=0.115.0      # REST API server
uvicorn[standard]>=0.30.0  # ASGI server (websockets, auto-reload)
